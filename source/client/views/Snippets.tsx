import React, { FC } from 'react';
import { BaseLayout } from '@/library/.ui/layouts';

export const SnippetsView: FC = () => {
    return (
        <BaseLayout>
            <h1>SnippetsView</h1>
        </BaseLayout>
    );
};
